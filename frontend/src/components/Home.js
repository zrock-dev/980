'use client';

import { GeneralButton } from '@/elements/GeneralElements';

const Home = () => {
	return (
		<div>
			<GeneralButton size={'20px'}>nose</GeneralButton>
			<img src="/plane-main-page.png" alt="plane-980" />
		</div>
	);
};

export default Home;
